# Workspace build log

Records **cross-repo coordination changes only**. Per-repo changes go in each repo's own `log.md`.

## [YYYY-MM-DD] workspace | living-documentation workspace layer established

- Created workspace-level `CLAUDE.md` and `knowledge/connections/`
- Per-repo `CLAUDE.md` files remain authoritative for each repo
- Workspace layer covers cross-repo concerns only
